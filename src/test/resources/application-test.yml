# 테스트는 외부 DB 없이 H2(PostgreSQL 호환 모드)로 돈다.
spring:
  datasource:
    url: jdbc:h2:mem:noriter;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:

server:
  port: 0

noriter:
  auth:
    secret: test-only-secret-that-is-long-enough-for-hs256!
  cors:
    allowed-origins: http://localhost:5173
  game:
    dev-options: false
