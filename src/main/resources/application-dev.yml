spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://127.0.0.1:5432/noriter_dev}
    username: ${DB_USER:noriter}
    password: ${DB_PASSWORD:noriter}

server:
  port: ${PORT:8081}

noriter:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:https://noriter-web-dev.asgd56.workers.dev,http://localhost:5173}
  game:
    dev-options: true
