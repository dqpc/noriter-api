spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://127.0.0.1:5432/noriter}
    username: ${DB_USER:noriter}
    password: ${DB_PASSWORD:noriter}

server:
  port: ${PORT:8080}

noriter:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:https://noriter-web.asgd56.workers.dev}
  game:
    dev-options: false
